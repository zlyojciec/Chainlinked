{
  "ignore": [
    "README.md",
	".gitignore"
  ],
  "commands": {
    "Compile": "./node_modules/.bin/truffle compile",
    "Migrate": "./node_modules/.bin/truffle migrate --reset --network development",
    "Test contracts": "./node_modules/.bin/truffle test",
    "Test dapp": "./node_modules/.bin/truffle test"
  },
  "hooks": {
    "post-unpack": "npm install && npm test"
  }
}